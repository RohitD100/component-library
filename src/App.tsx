import React from 'react'
import Spinner from './components/Spinner/Spinner'

const App = () => {
  return (
    <div>
      <Spinner size={20} thickness={5} color='red' speed={0.8}/>
    </div>
  )
}

export default App
